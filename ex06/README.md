# Borrowing and References

Borrowing and references are simular. You borrow a reference to a resource.

A reference can be:
- immutable
- mutable

Creating a reference is done by adding '&' before the variable.

See also [buffer](../buffer/) for an example using a thread safe buffer with multiple readers and writers where ownership and borrowing is crucial.