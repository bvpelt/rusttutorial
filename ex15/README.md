# Collection Types
- Vectors
- UTF-8
- Hash Maps

## Vectors
A Vector allows you to store multiple values of the same type next to each other in memory.

## UTF-8
All strings are just a bunch of bytes in memory. The String default rust package handles them as UTF-8 encoded characters.

## Hash Maps
A collection of key,value pairs.
